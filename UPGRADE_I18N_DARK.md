# 中英文与深色模式更新说明

本包是在你上传的最新版 `NCS_Charging-dev.zip` 上修改的完整项目，可以在 Windows + VS Code 中使用原来的 Python 启动方式运行。

## 新增功能

- **中文 / English**：登录与注册、用户端、管理端、操作弹窗、状态标签、表单提示、业务错误提示和 CSV 列标题支持切换。
- **浅色 / 深色 / 跟随系统**：全局适配导航、卡片、表格、输入框、图表和弹窗。首次使用默认跟随系统，可随时改成固定浅色或固定深色。系统主题变化时，“跟随系统”会同步更新。
- **自动记住设置**：浏览器先保存当前选择；登录后同步到账号。已有账号偏好会在下次登录时恢复。账号尚无偏好时，采用当前浏览器的选择。不同账号各自保存偏好。
- **切换入口**：登录页右上角、登录后的页面顶部均有语言与外观选择器；用户“个人中心”和管理员“偏好设置”也可以修改。
- **输入保留**：切换语言时保留登录、注册、个人资料等当前页面表单中尚未提交的输入。密码只在当前页面内存中保留，不写入偏好存储。
- **手机布局**：为顶部选择器与较长的英文标签增加小屏幕样式；实际手机显示仍请按下方步骤确认。

界面翻译文件随项目一起提供，不需要翻译 API、密钥或外部翻译服务。金额仍为人民币 CNY，不做汇率换算。用户昵称、电站名称、地址、停车说明、故障描述等业务数据保持原文，不自动改写；腾讯地图等外部页面由其自身语言设置决定。

## 已有项目如何升级

1. 停止正在运行的 Python 服务。
2. 备份现有项目和数据库。保留你本机的 `.env`、`data` 目录及 `.venv`；MySQL 用户还应备份现有数据库。
3. 解压本包，把内层 `NCS_Charging-dev` 中的源码文件覆盖到现有项目。不要删除或用示例配置覆盖你的实际 `.env`，不要删除原数据库。
4. 在 VS Code 打开**直接包含 `app.py`、`requirements.txt`、`ncs`、`static` 的文件夹**。终端也必须位于这个文件夹。
5. 使用原来的虚拟环境启动：

```powershell
.\.venv\Scripts\python.exe app.py
```

本次没有新增 Python 运行依赖。如果在全新文件夹安装，先执行：

```powershell
py -3 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

6. 用原来的地址打开系统。首次升级后在电脑浏览器按 **Ctrl + F5**，让新版脚本和样式加载。
7. 在页面顶部选择 `English` 和 `深色 / Dark`，刷新并退出重新登录，确认设置保留。

如果提示找不到 `requirements.txt`，先执行 `Get-Location` 和 `Test-Path .\requirements.txt`。结果为 `False` 表示终端不在项目根目录；通常需要再进入解压后的内层 `NCS_Charging-dev` 文件夹。

## 数据库兼容与首次启动

- 沿用上传版本默认的 **MySQL** 配置与环境变量；本更新没有把你的默认数据库改为 SQLite。
- SQLite 后端仍受支持，用于原有可选配置和自动测试。
- 启动时自动创建 `user_preferences` 表，按登录用户 ID 保存 `language`、`theme` 和更新时间。
- 这次迁移不重建用户、订单、电站等业务表，也不重置已有余额或订单。
- MySQL 连接账号需要拥有创建该表的权限。如果已有部署限制了建表权限，请由数据库管理员执行 `ncs/preferences.py` 内的 `MYSQL_SCHEMA` 建表语句，然后再启动。
- 首次使用一个全新空数据库仍会按原项目逻辑初始化演示数据。这与界面偏好功能无关。

## 验证记录与实机检查

已完成：

- 原有 17 项业务测试与新增 7 项偏好/翻译测试，共 **24 项全部通过**；使用独立临时 SQLite 数据库。
- **39 项页面脚本与接口联动检查通过**，覆盖中英文页面生成、用户与管理员页面、未提交表单保留、账号偏好恢复、CSV 语言参数、跟随系统主题变化及固定主题优先；使用 jsdom 执行页面脚本并连接真实 Flask 测试服务。
- 英文页面中剩余中文已核对为语言选择项“中文”和原始业务数据。
- Python/JavaScript 语法检查及最终 ZIP 完整性检查。

当前执行环境无法启动图形浏览器，因此 **jsdom 检查不代表真实浏览器排版或截图验收**。尚未完成 Windows、手机浏览器或真实 MySQL 服务的实机验证。`previews` 文件夹沿用上传版本的历史预览图，不是本次深色模式的新截图。

建议你在现有电脑上完成以下确认：

1. 分别登录用户与管理员，切换中英文和三种主题，检查导航、表格、弹窗与充电页面。
2. 选择“跟随系统”后改变 Windows 外观，观察页面自动切换；固定浅色/深色应保持所选外观。
3. 刷新、重新打开浏览器、退出后重新登录，确认账号偏好恢复。
4. 手机打开原有服务地址，检查顶部选择器、侧边栏、表格横向滚动；宽表格可在表格区域内左右滑动。
5. 确认升级前的余额、订单和电站记录仍然存在；深色模式下二维码保持黑白显示，打印小票使用浅色纸面。

可在项目根目录重新运行后端测试：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

## 代码位置

- `static/preferences.js`：浏览器偏好、语言字典加载、系统主题监听及首次绘制主题。
- `static/i18n/en.json`：中英文界面词条。
- `static/themes.css`：主题颜色与中英文、小屏幕适配样式。
- `static/app.js`：页面翻译、选择器、账号同步与切换时输入保留。
- `ncs/preferences.py`：MySQL / SQLite 偏好存储。
- `ncs/i18n.py`：后端错误、导出列名、已知操作日志的显示翻译。
- `tests/test_preferences.py`：新增回归测试。

原项目其他功能与部署文件继续随包提供；原来的 README、VALIDATION 和性能报告包含历史描述。本次变更与验证范围以本文为准。
