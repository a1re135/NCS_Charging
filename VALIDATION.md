# 验证记录

验证日期：2026-09-13。执行环境：Linux / Python 3.12 / SQLite / Flask 3.1.2 / Chromium 140。

## 业务回归：12 / 12 通过

1. 昵称、头像主题、按整数分充值在重新登录后保留。
2. 拒绝非有限数、负数、零、超额、超过两位小数的充值；拒绝缺少 CSRF Token 的写请求。
3. 同用户多个未完成订单拦截；其他用户不能占用已预约桩；取消后释放。
4. 预约超期后在下一次已登录 API 请求时释放。
5. 单价与功率快照、准确扣款和重复结算拒绝。
6. 余额不足记录欠费；欠费阻止新充电；充值补缴；拒绝重复补缴。
7. 普通用户不能访问管理接口，也不能读取或结算他人的订单。
8. 两个并发用户抢同一个桩，仅一个成功。
9. 两个并发结算请求，仅产生一次扣款流水。
10. 管理电站/电桩新增；使用中电桩受保护；有历史订单不能删除；故障与模拟恢复；操作日志。
11. 冻结用户不能充值或新开订单，但能结束已有充电。
12. 注册、按坐标排序、基础预测输出与 CSV 导出。

运行：`python -m unittest discover -s tests -v`。

## 浏览器流程

通过 Chromium 自动操作验证：用户登录 → 电站详情 → 预约 → 开始充电 → 结束并结算 → 小票；个人资料修改与保存；模拟充值；管理员登录及全部管理菜单加载；添加电桩表单打开；390 像素窄屏页面宽度检查。该流程未捕获 JavaScript 运行错误。

界面截图在 `previews/`。截图数据来自独立示例数据库，不代表真实运营。

## 未实机验证

- Windows 的批处理执行和 VS Code GUI（当前执行环境是 Linux）。
- 腾讯地图外部路线服务、用户电脑浏览器定位授权。
- 真实短信、支付和硬件未接入；不属于本版的验证范围。

Windows 启动说明见 README.md；核心代码不依赖 Linux 专用 API。测试不修改用户运行数据库。
