# 验证记录

验证日期：2026-09-13。执行环境：Linux / Python 3.12 / SQLite / Flask 3.1.2 / Chromium 140。

## 业务回归：12 / 12 通过

1. 昵称、头像主题、按整数分充值在重新登录后保留。
2. 拒绝非有限数、负数、零、超额、超过两位小数的充值；拒绝缺少 CSRF Token 的写请求。
3. 同用户多个未完成订单拦截；其他用户不能占用已预约桩；取消后释放。
4. 预约超期后在下一次已登录 API 请求时释放。
5. 单价与功率快照、准确扣款和重复结算拒绝。
6. 余额不足记录欠费；欠费阻止新充电；充值补缴；拒绝重复补缴。
7. 普通用户不能访问管理接口，也不能读取或结算他人的订单。
8. 两个并发用户抢同一个桩，仅一个成功。
9. 两个并发结算请求，仅产生一次扣款流水。
10. 管理电站/电桩新增；使用中电桩受保护；有历史订单不能删除；故障与模拟恢复；操作日志。
11. 冻结用户不能充值或新开订单，但能结束已有充电。
12. 注册、按坐标排序、基础预测输出与 CSV 导出。

运行：`python -m unittest discover -s tests -v`。

## 浏览器流程

通过 Chromium 自动操作验证：用户登录 → 电站详情 → 预约 → 开始充电 → 结束并结算 → 小票；个人资料修改与保存；模拟充值；管理员登录及全部管理菜单加载；添加电桩表单打开；390 像素窄屏页面宽度检查。该流程未捕获 JavaScript 运行错误。

界面截图在 `previews/`。截图数据来自独立示例数据库，不代表真实运营。

## 未实机验证

- Windows 的批处理执行和 VS Code GUI（当前执行环境是 Linux）。
- 腾讯地图外部路线服务、用户电脑浏览器定位授权。
- 真实短信、支付和硬件未接入；不属于本版的验证范围。

Windows 启动说明见 README.md；核心代码不依赖 Linux 专用 API。测试不修改用户运行数据库。

## 新增功能验证记录（2026-09-14，Windows / Python 3.11 / Node 22）

1. 充电桩状态新增“维修中 maintenance”“离线 offline”：管理端电桩管理可切换故障/维修/离线/恢复；维修与离线桩不可预约和开始充电；总览与详情状态徽章、状态筛选同步支持。
2. 订单按创建日期筛选：用户“我的订单”与管理端“订单管理”均可选起止年月日，并提供今天/近 7 天/近 30 天/全部快捷按钮；管理员 CSV 导出同步应用日期范围；非法或起止颠倒的日期返回 400。
3. 充电站筛选：电站列表（用户与管理端共用）支持充电状态（有空闲/故障/维修中/离线）筛选，并按距离或充电次数排序；电站详情页支持按电桩状态、类型筛选并按充电次数排序。电站管理页原有“全部类型/快充/慢充”类型下拉已按需求移除（该处搜索无意义）。

验证方式与结果：

- 业务回归测试 16 / 16 通过（原 12 项 + 新增 4 项：`test_charger_maintenance_and_offline_status`、`test_station_filters_and_usage_sort`、`test_order_date_filter_and_export`、`test_admin_user_status_filters_and_sort`），运行 `python -m unittest discover -s tests -v`。
- 独立临时数据库 + 5055 端口实跑服务，HTTP 端到端 30 项检查全部通过（含新接口参数、权限、CSRF 与静态资源）。
- 前端 `static/app.js` 通过 `node --check` 语法检查；新增页面逻辑（电站/电桩筛选、订单日期筛选、快捷按钮）以 DOM 桩在 Node 下运行时冒烟 16 项全部通过。
- 对现有 `data/ncs.db` 只读执行新查询，兼容正常；旧库中不会自动出现维修中/离线桩与示例欠费订单，需管理员在电桩管理手动设置、或用户欠费后自然出现（全新初始化数据库会自动预置示例维修中/离线桩，并为“小明”预置一笔欠费订单）。

## 管理端第二轮功能验证记录（2026-09-15，Windows / Python 3.11 / Node 22）

1. 电站管理：删除“全部类型/快充/慢充”类型下拉筛选（该处按类型筛选无意义）；排序文案由“使用率最高（充电次数最多）”改为“充电次数最多”，仅保留“按距离排序 / 充电次数最多”两项。
2. 电桩管理：新增所属电站下拉筛选（按电站查看该站全部电桩）、类型筛选（快充/慢充），与既有状态筛选、搜索叠加生效；新增排序“按编号排序 / 充电次数高到低 / 充电次数低到高”。
3. 用户管理：状态三态化——正常（无欠费且未冻结）/ 欠费（存在未补缴订单欠费，`debt_cents>0`）/ 冻结（被管理员停用），表格新增“欠费”列（显示欠费金额或 —），欠费用户以粉色、冻结用户以灰色徽章区分；新增状态筛选、注册时间起止日期区间筛选（含近 30 天 / 全部快捷按钮）、按最新注册/最早注册排序；日期非法或起止颠倒返回 400。

验证方式与结果：

- 业务回归测试 16 / 16 通过（新增 `test_admin_user_status_filters_and_sort`：欠费判定、状态筛选、注册日期区间、最新/最早排序、非法参数 400、起止颠倒 400）。
- 独立临时数据库 + 5055 端口实跑服务，HTTP 端到端 23 项检查全部通过（用户接口三态筛选与排序、日期参数校验、电站/电桩列表兼容、冻结/启用联动）。
- 前端 `static/app.js` 通过 `node --check` 语法检查；新增页面逻辑（电站去类型、电桩站/型/态筛选与三排序、用户三态徽章/筛选/日期/排序、查询参数组装）以 DOM 桩在 Node 下运行时冒烟 28 项全部通过。
- 对现有 `data/ncs.db` 只读执行新查询，兼容正常。

## 第三轮新增功能验证记录（2026-09-15，Windows / Python 3.11 / Node 22 / 双数据库）

1. 欠费订单闭环：我的钱包“待补缴金额 → 查看欠费订单”改为弹窗，列出全部欠费订单（订单号、电站/电桩、欠费金额、创建时间），可查看小票详情或直接在线补缴；补缴成功后弹窗即时刷新。“我的订单”与订单管理状态列新增“欠费”状态（已完成但存在未补缴欠费的订单显示“欠费”而非“已结算”），状态筛选下拉同步新增“欠费”。
2. 营收统计：新增各电站总营收柱状图（按实收从高到低排序）、所有电站平均实收指标；与“最近 7 天营收”并列展示。
3. 后台首页订单趋势图：与收入趋势图并列展示；时间粒度可按天/按周/按月，范围近 7 天/近 30 天/本年度，可指定单个电站或全部电站；订单口径可切换“有效完成/全部订单”；数据与订单管理同源（orders 表实时聚合）。
4. 快慢充数量显示：用户电站列表卡展示“快充 N 个（空闲 M）｜慢充 N 个（空闲 K）”，并提供“只看快充/只看慢充”勾选过滤；电站详情页分区统计快充/慢充总数与空闲数；管理端电站卡按快充/慢充展示空闲、故障、离线分型数量；后台首页展示平台快充/慢充总量及空闲、故障分型统计。
5. 冻结用户死循环修复：冻结用户现在可以充值、可以补缴欠费（结算与补缴放行），但仍不能发起新订单。
6. 订单管理“已结算”修正：欠费订单状态列显示“欠费”。

验证方式与结果：

- 业务回归测试 38 / 38 通过（新增 `test_frozen_user_can_recharge_repay_but_not_new_order`、`test_station_kind_breakdown_fields`、`test_order_trend_endpoint`、`test_revenue_stations_endpoint`，共 24 项覆盖第四轮前功能）。
- 独立临时数据库 + 5055 端口实跑服务，HTTP 端到端 30 项检查全部通过（趋势各粒度/范围/电站参数与 400/404/403 校验、各站营收排序与平均、电站分型字段、冻结充值/补缴/新订单拦截）。
- 前端 `static/app.js` 通过 `node --check` 语法检查；以 DOM 桩在 Node 下运行时冒烟 27 项全部通过（欠费状态判定、快慢充分区统计、电站类型过滤、趋势图渲染、欠费订单弹窗、电站列表/订单页/后台首页/营收页渲染）。
- 对真实 MySQL（`ncs_charging`）只读执行本轮全部新 SQL（电站分型聚合、按周/按日趋势、各站营收），语法与结果全部通过，确认双后端兼容。

## 第四轮功能验证记录（2026-09-15，Windows / Python 3.11 / Node 22 / 双数据库）

1. 钱包补缴即时刷新：从“我的钱包 → 查看欠费订单”弹窗补缴成功后，可用余额与待补缴金额同步更新（此前需手动刷新）；直接更新余额卡与欠费汇总，无需整页重载。
2. 后台首页“平台资源”数字异常修复：根因是 MySQL 对 SUM/AVG 返回 DECIMAL，Flask 默认 JSON 序列化为字符串，前端 `reduce` 累加变成字符串拼接（如“快充 08866666888 个”）。新增全局 JSON 提供器把 DECIMAL 序列化为数字（整数/浮点），已对真实 MySQL 验证 `fast=70, slow=30` 等均为数字。
3. 电站管理：删除“按距离排序”，改为“充电次数最多 / 充电次数最少”（管理端、运维端可见）；用户端“附近电站”仍保留“按距离排序 / 充电次数最多”。
4. 左侧导航删除“偏好设置”栏目（仅保留点击头像后的个人主页内的偏好设置）；删除“价格管理”栏目（并入电站详情的“分时收费标准”，管理员/运维可在电站详情内新增、编辑、删除价格时段，价格表单默认选中当前电站）。
5. 左侧导航保持滚动位置：点击底部栏目后不再回弹到顶部。
6. 用户“附近电站”：去掉“只看快充/只看慢充”勾选；状态下拉仅“全部状态 / 有空闲桩”（故障/维修中/离线不再单列，但“全部状态”下仍显示这些电桩）。电站详情：状态下拉仅“全部状态/空闲/预约中/充电中”（选“全部”仍可见故障/维修中/离线桩）；类型由下拉改为“快充/慢充”勾选框（不勾选=快慢充全部显示）。

验证方式与结果：

- 业务回归测试 38 / 38 通过（routes 排序新增 `usage_asc`、全局 DECIMAL JSON 提供器改动后无回归），运行 `python -m unittest discover -s tests`。
- Node DOM 冒烟 22 项全部通过（导航去 pricing/settings、电站列表用户/管理端状态与排序差异、详情状态下拉裁剪、类型勾选过滤四组行数、管理员价格操作列与“＋ 添加价格时段”、补缴后余额/待补缴即时更新、侧栏滚动位置保留、平台资源数字累加）。
- HTTP 端到端 10 项全部通过：独立临时 SQLite 验证 `sort=usage_asc` 升序与 `sort=usage` 降序；真实 MySQL（`ncs_charging`，只读）验证 stations 聚合字段（fast/slow/fast_free/slow_free/fast_fault/slow_fault/usage）全部为数字而非字符串、平台快充 70/慢充 30 累加正常、dashboard totals、各站营收 revenue_cents、趋势 cents 均为数字。
- `static/app.js` 通过 `node --check`，`static/i18n/en.json` 通过 JSON 校验（新增“充电次数最少”词条，共 567 条）。
- 未纳入本轮改动范围（未触碰）：订单/营收/趋势接口逻辑、欠费弹窗业务、用户管理三态等，保持第三轮验证结论。

## 第五轮调整验证记录（2026-09-15，Windows / Python 3.11 / Node 22 / 双数据库）

1. 电站详情快充/慢充勾选美化：原生复选框改为胶囊切换按钮（隐藏原生 checkbox，样式化 `span` + 选中渐变高亮，快充=紫蓝、慢充=青绿，带圆点指示与阴影），保留原 checkbox 事件机制，过滤逻辑不变。
2. 运营总览平台资源数字：后端 DECIMAL→数字序列化已确认生效（真实 MySQL：fast=70 / slow=30 / fast_free=69 / slow_free=27 均为数字）；前端 `reduce` 累加再增加 `Number()` 兜底，即使接口返回字符串数字也不再拼接（Node 冒烟用字符串 `'6'`/`'4'` 输入验证累加得 60/40）。
3. 电站详情“分时收费标准”拆为独立卡片（位于“电站信息”与“选择你的充电桩”之间），结构为 3 张卡片：电站信息 / 分时收费标准 / 选择你的充电桩。
4. “充电次数最少”排序：后端 `usage_asc` 校验与排序分支确认已生效，真实 MySQL 实测 `/api/stations?sort=usage_asc` 返回 200 且按 usage 升序（样例 [0,0,0,0,1,24]），`sort=usage` 降序正常——此前提示“排序方式无效”为旧后端进程未重启所致，重启服务后即恢复。

验证方式与结果：

- Node DOM 冒烟 9 项全部通过（胶囊结构与 class、卡片数量与顺序、字符串数字兜底累加、无拼接痕迹、胶囊勾选过滤仍生效）。
- 真实 MySQL（`ncs_charging`，只读）HTTP 端到端 7 项全部通过（usage_asc 200/升序/数值、usage 降序、kind 字段全为数字、dashboard totals 数字）。
- `static/app.js` 通过 `node --check`；`static/style.css` 语法替换锚点唯一命中。
- 注意：第 2、4 项为后端进程级改动（`ncs/__init__.py`、`ncs/routes.py`），需重启 Flask 服务后生效；前端改动需浏览器硬刷新（Ctrl+F5）加载最新 `app.js`/`style.css`。

